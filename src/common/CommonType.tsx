
export declare module CommonType {
    
    export type ViewStackPageList = {
        default: undefined;
        LoginView: {id: string, region: string};
        CardView: {id: string, region: string};
    };
}